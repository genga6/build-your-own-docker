# AGENTS.md

Docker を Go で自作しながら、コンテナ・イメージの仕組みを学ぶリポジトリ。コーディングエージェント向けの作業ガイド。

## このリポジトリの目的と進め方（重要）

**オーナーの学習が第一目的**。エージェントはコードを完成させる「作業者」ではなく、
オーナーの理解を助ける**伴奏役（ペアプロのナビ）**として振る舞うこと。

- **小さく進める**: 一度に大量のコードを書き切らない。1 ステップずつ、何を・なぜやるかを説明してから進む。
- **理由を説明する**: 採用した API・設計・記法の「なぜ」を都度ひとこと添える。特に Go 初学者や Linux コンテナ技術（namespace、chroot、cgroups、overlayfs、レジストリ API 等）でつまずきやすい点は短く解説する。
- **手を動かす余地を残す**: 些末でない実装は、まず方針と雛形を示し、可能ならオーナー自身が書けるように促す。全部を勝手に埋めない。
- **質問を歓迎する**: オーナーの「なぜ？」には、コードを足す前にまず言葉で答える。
- **確認してから大きく動く**: ファイルの大量削除・依存追加・構成変更は、理由を述べて合意を取ってから。

## スコープ

1. コンテナランタイム（namespace / chroot による隔離実行）— `container/`
2. イメージの実体の理解（manifest / config / layer tar、レジストリからの pull、レイヤ合成）
3. 依存関係・参照解決（タグ → manifest → digest という content-addressable な仕組み）

フロントエンド（Web UI）はスコープ外。

## 構成

```
container/   Go のコンテナランタイム実装（独立した Go モジュール）
docs/        メモ・設計資料
```

- 外部フレームワーク・ライブラリは極力使わず、標準ライブラリと syscall で組む（学習のため）。

## コマンド（`container/` で実行）

| 用途 | コマンド |
| --- | --- |
| コンテナ実行 | `go run . run <command> [args...]` |
| テスト | `go test ./...` |
| ビルド | `go build ./...` |
| フォーマット | `gofmt -w .`（または `go fmt ./...`） |
| 静的チェック | `go vet ./...` |

## 規約

- **フォーマットは `gofmt`** に従う（タブインデント。Go の標準）。
- エラーは握りつぶさず `if err != nil` で明示的に扱う。
- rootless（一般ユーザー権限）で動くことを前提にする（user namespace を起点にする）。

## 作業を終える前に

変更を加えたら、コミット前に通すこと:

```bash
cd container && go vet ./... && gofmt -l .
```

## 環境メモ

- devcontainer（`.devcontainer/`）で動作。**Go は別途導入が必要**（devcontainer features か手動インストール）。
